# Motion Runtime

Motion owns axis state, homing, limits, profiles, synchronization, and fault transitions. Motion resources and timing are reserved. Any change affecting motion requires simulation/validation and human approval before deployment.

