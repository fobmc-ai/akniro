# Agent Permissions

Agents are scoped by identity, tenant/site/machine, tools, read/write domains, context levels, resource budget, expiry, and approval policy. Multiple agents are isolated by default. An agent cannot grant itself permission, access another tenant, bypass audit, or invoke undeclared tools.

