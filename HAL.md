# Hardware Abstraction Layer

HAL exposes stable primitives for clock, memory, IO, fieldbus, camera, GPU/NPU, storage, network, and safety status. Hardware-specific behavior is isolated behind adapters and manifests. HAL APIs are versioned and testable with simulation doubles.

