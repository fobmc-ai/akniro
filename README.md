# Runtime Boundary

Owns live execution, realtime scheduling, IO image, runtime state, health, watchdog, and degraded mode. Runtime MUST remain isolated from browser, cloud, AI, and unbounded non-deterministic work.

