# Hardware Manifest

Hardware Manifest declares architecture, CPU cores, memory, storage, GPU/NPU, network interfaces, fieldbus, IO, camera, serial/USB, firmware, secure boot, thermal/power limits, and resource topology. Software selects capabilities from the manifest; it must not scatter hardware-specific conditionals.

