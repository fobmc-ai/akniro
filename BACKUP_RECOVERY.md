# Backup and Recovery

Backup includes project revision, runtime/app versions, device and hardware manifests, configuration, recipes, drivers, licenses as recoverable references, migrations, and integrity metadata. Restore validates target compatibility, applies migrations, verifies health, and records evidence. Recovery must work through a documented offline path.

