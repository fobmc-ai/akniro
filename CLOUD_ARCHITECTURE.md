# Cloud Architecture

Cloud provides fleet, identity federation, policy distribution, analytics, collaboration, remote access, and update orchestration. Edge remains authoritative for minimum machine operation and local safety behavior.

