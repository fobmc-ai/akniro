# AI Cost Policy

Prefer local indexes, cached summaries, small models, and deterministic tools for navigation. Deduplicate context by content hash. Record model, input/output budget, retrieval sources, and cost class. Escalate model or context only when the expected quality gain justifies it.

