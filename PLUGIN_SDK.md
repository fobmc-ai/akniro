# Plugin SDK

Plugins extend declared extension points only. SDKs provide versioned interfaces, sandboxing, lifecycle hooks, health checks, permissions, resource declarations, and compatibility tests. Plugin code must not bypass policy or access private platform storage.

