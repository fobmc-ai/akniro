# Machine Project

Machine Project is the unit of source, validation, deployment, backup, AI context, and audit. It contains model metadata, programs, HMI, vision, motion, recipes, mappings, dependencies, policies, manifests, and release history.

Project revisions are immutable after release. Working copies require branch/merge semantics or equivalent conflict handling. A deployment references exactly one signed project revision.

