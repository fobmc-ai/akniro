# Time Model

Use monotonic time for intervals and synchronized UTC for cross-system ordering. Preserve source timestamp, ingest timestamp, clock quality, timezone presentation, and sequence where available. Time uncertainty is data, not an implementation detail.

