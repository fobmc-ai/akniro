# Remote Access

Remote access is time-bound, least-privilege, operator-visible, auditable, and disabled by default for critical control paths. Remote observation and remote control are distinct permissions.

