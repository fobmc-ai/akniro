# Performance

Define latency, jitter, throughput, startup, memory, CPU, storage, and recovery budgets per capability. Realtime budgets are measured under worst-case declared load, not average load. AI and analytics have quotas and must yield to critical workloads.

