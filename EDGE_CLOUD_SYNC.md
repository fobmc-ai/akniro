# Edge/Cloud Sync

Sync is queued, authenticated, resumable, ordered where required, and conflict-policy driven. Local writes carry origin and revision metadata. Cloud outage must not block declared edge operations.

