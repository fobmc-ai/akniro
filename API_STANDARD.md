# API Standard

APIs use explicit versioning, stable error types, idempotency keys for commands, pagination, correlation IDs, authorization context, schema evolution rules, and deprecation dates. Breaking changes require a new major contract and migration path.

