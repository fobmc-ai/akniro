# Device Model

Device Registry owns identity, vendor/model, capabilities, firmware, connection references, health, topology, and replacement identity. Drivers own transport sessions and diagnostics, not canonical device records.

