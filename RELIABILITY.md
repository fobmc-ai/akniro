# Reliability

Every service declares failure modes, health, watchdog, restart, persistence, backpressure, retry, idempotency, degraded mode, and recovery. Prefer fail-safe or fail-operational behavior according to the machine risk assessment.

