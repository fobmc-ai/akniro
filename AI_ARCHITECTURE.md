# AI Architecture

AI is a replaceable platform service composed of model gateway, retrieval/indexing, machine context, tool policy, evaluation, audit, and approval workflow. No model is a source of truth. Machine Context belongs to the platform and is model-independent.

