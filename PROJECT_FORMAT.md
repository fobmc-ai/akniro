# Project Format

The project format is a versioned directory/package with a manifest, canonical schemas, content-addressed assets, dependency lock, signatures, migration metadata, and generated index. Unknown fields must be preserved where safe; unknown required versions must fail clearly.

