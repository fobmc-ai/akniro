# Recipe Model

Recipes are versioned, validated, approved parameter sets. Draft, review, approved, released, archived, and revoked are separate states. Runtime receives a signed release and reports the applied revision.

