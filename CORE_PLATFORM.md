# Core Platform

The Core Platform provides canonical domain services and cross-cutting contracts. It owns identity, project metadata, capability discovery, resource leases, event envelopes, time, audit, and lifecycle policy.

Core MUST be transport-neutral and deterministic where its contract is used by realtime consumers. Core MUST NOT embed UI or vendor protocol logic.

