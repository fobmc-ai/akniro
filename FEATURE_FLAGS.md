# Feature Flags

Flags are typed, scoped, auditable, time-bounded, and default-safe. A flag cannot bypass safety, authorization, migration, or resource checks. Every flag has an owner and removal date.

